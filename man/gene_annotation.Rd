% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gene_annotation.R
\name{gene_annotation}
\alias{gene_annotation}
\title{Annotate variant-level pleiotropy scores to genes}
\usage{
gene_annotation(
  data,
  gene_loc_file = NULL,
  magma_bin = "magma",
  window_up = 10,
  window_down = 10,
  outfile = getwd()
)
}
\arguments{
\item{data}{A data.frame or data.table containing a `snp` column and one or
more variant-level score columns, such as the output of [getHOPS()].}

\item{gene_loc_file}{Path to a MAGMA-compatible gene-location file.
Default is based on gene locations obtained from the NCBI site
(NCBI 37.3, 19,427 protein-coding genes).}

\item{magma_bin}{Path to the MAGMA executable, or `"magma"` when MAGMA is
available on the system PATH.}

\item{window_up}{Upstream annotation window in kilobases. Default is `10`.}

\item{window_down}{Downstream annotation window in kilobases. Default is `10`.}

\item{outfile}{Directory in which MAGMA output files are written.
Default is the current working directory.}
}
\value{
A data.table with one row per mapped gene--SNP pair. It includes
gene ID, gene symbol, genomic coordinates, strand, SNP ID, and all
variant-level score columns supplied in `data`.
}
\description{
Maps variants to genes with MAGMA and merges the resulting gene-SNP
annotations with variant-level pleiotropy scores.
}
\details{
MAGMA output files, including `variant2gene.genes.annot`, are written to
`outfile`. Variants absent from the packaged LD-score reference are excluded
before MAGMA annotation and reported in a warning.
}
\examples{
\dontrun{
variant2gene <- gene_annotation(
  data = variant_score,
  magma_bin = "magma",
  window_up = 10,
  window_down = 10,
  outfile = tempdir()
)
}

}
